# Include this file inside the eSchool server block. The ^~ prefix prevents
# the generic PHP location from receiving anything below public uploads.
location ^~ /storage/ {
    try_files $uri =404;
}

location ^~ /uploads/ {
    try_files $uri =404;
}
