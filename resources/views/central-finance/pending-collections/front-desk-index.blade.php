@extends('layouts.master')
@section('title', __('Pending collections'))
@section('content')
@include('central-finance.partials.data-visibility-toggle')
<div class="container-fluid py-3"><div class="card"><div class="card-body"><p class="text-uppercase text-muted mb-1">{{ __('School Finance') }}</p><h3>{{ __('My pending collections') }}</h3><p class="text-muted">{{ $school->name }} · {{ __('A pending acknowledgement is not an official receipt.') }}</p><div class="table-responsive"><table class="table cf-data-table"><thead><tr><th>{{ __('Acknowledgement') }}</th><th>{{ __('Student') }}</th><th>{{ __('Amount') }}</th><th>{{ __('Status') }}</th><th>{{ __('Submitted') }}</th><th>{{ __('Receipt') }}</th></tr></thead><tbody>@forelse($pending as $row)<tr><td>{{ $row->acknowledgement_no }}</td><td>{{ $row->studentProfile?->student_name }}</td><td>{{ number_format($row->amount,2) }} {{ $row->currency }}</td><td><span class="badge cf-status-badge badge-light">{{ $row->status === 'confirmed' ? __('Finance Confirmed') : __('Collected – Pending Finance Confirmation') }}</span></td><td>{{ $row->submitted_at?->timezone('Asia/Yangon')->format('Y-m-d H:i') }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('central-finance.pending-collections.collection-receipt', $row) }}">{{ __('View / Print') }}</a></td></tr>@empty<tr><td colspan="6" class="text-muted">{{ __('No pending collections.') }}</td></tr>@endforelse</tbody></table></div>{{ $pending->links() }}</div></div></div>
@endsection
